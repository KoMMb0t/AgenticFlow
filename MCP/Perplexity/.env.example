# AgenticFlow MCP Server Umgebungsvariablen

# Basis-URL von AgenticFlow API
AGENTIC_FLOW_URL=http://localhost:3001

# Timeout für API-Calls (in Millisekunden)
AGENTIC_FLOW_TIMEOUT=30000

# Debug-Modus (verbose logging)
DEBUG=false

# MCP Server Port (falls Expose nach außen)
MCP_PORT=3000
